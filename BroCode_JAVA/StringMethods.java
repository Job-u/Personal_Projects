
public class StringMethods {

	public static void main(String[] args) {
		
		String name = "Job";
		
		int result = name.indexOf("J");
		
		System.out.println(result);
		
		
		/*
		 * boolean result = name.equals("Job"); //name.equalsIgnoreCase if you want to ignore case
		 * int result = name.length(); //Counts the total letters
		 * char result = name.charAt(0); //Defines what char in it
		 * boolean result = name.isEmpty(); //Checks if it is empty
		 * String result = name.toUpperCase(); //Turns all to upper case
		 * String result = name.toLowerCase(); //Turns all to lower case
		 * String result = name.trim(); //Removes all the empty spaces
		 * String result = name.replace('J','D'); //Replaces J to D
		 */
		
		
		
		

	}

}
