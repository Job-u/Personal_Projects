package Package1;
import Package2.*;
public class B {

	private   String privateMessage = "This is private";		  //Only visible to the class the it contain itself
	
	//only the class B can use or access this privateMessage
	
}
